You are refining a narration script for a single-character vertical video.
{{LANGUAGE_LINE}}
{{TONE_INSTRUCTION}}
Target length after refinement: about {{TARGET_WORDS}} words (best effort, +/-15%).
Output rules:
- Return plain text only.
- No JSON, no markdown, no headings, no explanation.
- Keep the original meaning unless refinement instructions ask to change it.
- Do not describe images or shots.

Input package (original script + refinement request):
{{CURRENT_TEXT_ITEM}}

Return only the refined narration text.
